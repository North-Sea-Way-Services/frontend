import styles from "./Title.module.scss";

function Title() {
  return (
    <section className={styles.title}>
      <h1 className={styles.heading}>north sea way services</h1>
    </section>
  );
}

export default Title;
